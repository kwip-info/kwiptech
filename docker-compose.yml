services:
  web:
    build: .
    command: sh /app/docker-entrypoint.sh
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      DEBUG: "true"
      ALLOWED_HOSTS: localhost,127.0.0.1
volumes:
  static_volume:
