# KWIP slideshow runtime; use Heroku config vars in production.
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1
# Set an unpredictable SECRET_KEY in production. No database or API keys required.
# Historical marketplace variables can remain in Heroku for rollback; show/ ignores them.
